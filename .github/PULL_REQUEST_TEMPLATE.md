## What changed

<!-- Files + behavior. One sentence per logical change. -->

## Why

<!-- Link the issue: fixes #N -->

## How was it tested

<!-- Exact commands run (e.g. ./gradlew assembleDebug) and manual steps. "It builds" alone is not enough for behavior changes. -->

## Checklist

- [ ] `./gradlew assembleDebug` passes locally
- [ ] No unrelated files in the diff
- [ ] New behavior is covered in README/Wiki if user-facing
- [ ] No secrets, keystores, personal paths in the diff
